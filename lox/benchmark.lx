fun benchmark() {
    fun fib(n) {
        if (n <= 1) return n;
        return fib(n - 2) + fib(n - 1);
    }

    var start = clock();
    var fib5 = fib(5);
    var end = clock();
    print("time: " + (end - start));
}

benchmark();
